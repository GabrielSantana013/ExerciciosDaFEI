/*
1- Desenvolver um algoritmo em JavaScript que exiba os números ímpares de 1 a 100
*/

/* 
for(let i = 0; i <=100; i++)
{
    if(i%2) // 1 é true 0 é falso
    {
        console.log(i);
    }

}
*/

/*
2- Faça um programa para imprimir os múltiplos de 5, no intervalo de 1 até 500
*/

/*
for(let i = 0; i<=500; i++)
{
    if(i%5 == 0)
    {
        console.log(i);
    }
}
*/

/*
3-Faça um programa em JavaScript que leia um número inteiro positivo e mostre todos os números a partir dele até zero (decrescente).
*/

/*
let num = prompt("Digite um número positivo: ");

for(let i = num; i >= 0; i--)
{
    console.log(i);
}
*/

/*
4- Desenvolver um algoritmo em JavaScript que exibe o resultado do fatorial de um número inteiro informado pelo usuário
*/

/*
let num = prompt("Digite um número positivo: ");
let acumulado = 1;

for(let i = num; i > 0; i--)
{
    acumulado *= i;
    console.log(acumulado);
}

console.log(acumulado);
*/

/*
5-Foi realizada uma pesquisa entre os habitantes de uma dada região.
Foram recolhidos os dados de idade, sexo (M/F) e salário. Construa um programa em JavaScript que informe:
a-) a média de salário do grupo; 
b-) maior e menor idade do grupo; 
c-) quantidade de mulheres com salário até R$500,00. 
Encerre a entrada de dados quando for digitada uma idade negativa.

*/

